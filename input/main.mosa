use native fn print#"mosa-native~>print";
use native fn printLn#"mosa-native~>printLn";

enum ExampleEnum {
   First,
   Second
}

let a: ExampleEnum = ExampleEnum/>Second;

let f = typeof a;
f ->> printLn;

if a == ExampleEnum/>Second {
    "True!" ->> printLn;
} else {
    "False!" ->> printLn;
}